name: Security Review

on: pull_request

permissions:
  pull-requests: write  # allows posting inline findings on the PR
  contents: read

jobs:
  security:
    uses: Ashmere-Software/.github-security/.github/workflows/security-review.yml@c46a22c78c93889836cc25b63437a45faf4cb106 # main
    secrets:
      CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
