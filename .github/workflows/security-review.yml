name: Security Review

on: pull_request

permissions:
  pull-requests: write  # allows posting inline findings on the PR
  contents: read

jobs:
  security:
    uses: Ashmere-Software/.github-security/.github/workflows/security-review.yml@bca8e4411a91591abff7a0f6acdd51337a6bd461 # main
    secrets:
      CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
